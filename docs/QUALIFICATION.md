# Names v1 qualification record

This record retains the pre-Zakura Names v1 baseline and the current
Zakura-backed release-finalization evidence. Historical values are kept for
lineage; the current section below is the authoritative artifact and live
qualification record. All qualification is local regtest evidence from the
pinned Zakura/Zaino/devtool stack; it is not a public deployment, an
independent security audit, or a guarantee for future hosts.

## Historical deterministic artifact freeze

- CNV1 revision: `0x01`
- vector-set identity (length-prefixed canonical envelopes):
  `dff01501326305709dc1eda3241a92458ce17a3461b6dd254c7f8f841a6932b1`
- `names_v1_wire.json` file SHA-256:
  `0aeb8795386c47f235375a648b0a3c512e75c8f3d9a5b40ae8c224d0807ef40a`
- state-note transition VK identity:
  `5ed1a1385f15e0e13e284cf1a7c319449d42b4902abc57b5ebefb60d04995cc1`
- state-note genesis VK identity:
  `81aa1ade09b0ca86eb80c021a66e2cf629875ecab258a99a4a2ecd0df2c7f5ae`

The historical VK identities were regenerated by
`state_note_binding::keygen()` under the then-pinned `K = 11` and Halo2
`0.3.2` parameters and rechecked by the frozen identity test. The historical
CNV1 vectors were regenerated from the canonical operation fixtures and
passed byte-for-byte conformance.

## Historical live qualification

The run used `zcash-devtool/scripts/live-qualification.sh --phase 2
--keep-state` from the v1 tooling source, with the pinned local stack:

- `coppice-names`: `92dd3a5c32ca3335dd12862137194344919e16a0`
- `zcash-devtool`: `3a1727300da533c62138fd4de03e4c36faafe533`
- `orchard-coppice`: `5588c4e42d7158233a50471c04340ea58615bb0e`
- Zakura: `f892b9074002a04a678ef2365ec7658795796572`
- Zaino: `b819583a1a6663a01cb7681ac5b5fc2a174596a0`
- preserved evidence: `/tmp/coppice-names-v1-live.oW430d`

Canonical lifecycle evidence:

- COMMIT `90243039f0977b1ac803ad2115d51db121430ce3ef6aa7fbb31f4ad25eec77a5`
  at height 14.
- REVEAL `b0376ee8e9f04992015d3a21f92e7cf554234ccd9f1f3f9b93226fabe61282ff`
  at scheduled height 19; replay and FreshResolver returned `Active`.
- UPDATE `41473053065d42ad7194b8029d3ee98e9b7d03ad236972cc6cd14c64bf6c6036`
  at height 20; replay and FreshResolver returned `Active`.
- RENEW `7884439913c95f6f87b80b6b588c549eb0f1843870529a67b3e60e72f3e7b588`
  at scheduled height 30; replay and FreshResolver returned `Active`.
- RELEASE `36d382260e7d2868638af1075d5a2c3016e57b88159e426c5c6f4387be53460c`
  at height 31; replay and FreshResolver returned `Released`.

Every lifecycle verification reported `NAMES_FULL_FRESH_MATCH=yes`. The exact
claimability boundary was also verified: `Released` at height 34 and `Expired`
at height 35 (`terminal_height 31 + reuse_delay 4`), with replay and
FreshResolver in agreement at both heights.

The run used 11 CPV1 frames per state operation and recorded 4,704-byte Names
proofs. These are observational release evidence, not optimization targets.
Proof-size and performance work, including evaluation of the Zakura Common
stack, remains outside this migration and follows final artifact
regeneration/requalification.

## Historical release status

The pre-migration v1 source, CNV1 vectors, VK identities, and complete local
live lifecycle were frozen and qualified. The historical `v1.0.0` tags are
preserved unchanged. The current Zakura-backed artifact and qualification are
recorded below and must be released under a new tag if publication is desired.

## Current Zakura-backed release qualification: 2026-08-30

The current source was regenerated and requalified after the Zakura migration.
The protocol reset intentionally rebranded the corrected v2 design as Names v1,
so the final wire artifact is CNV1 revision `0x01` (not the superseded CNV2
revision). The deterministic generators produced the existing checked-in bytes
exactly; no wire bytes changed during this campaign.

Deterministic artifact checks:

- transition VK identity: `5ed1a1385f15e0e13e284cf1a7c319449d42b4902abc57b5ebefb60d04995cc1`;
- genesis VK identity: `81aa1ade09b0ca86eb80c021a66e2cf629875ecab258a99a4a2ecd0df2c7f5ae`;
- CNV1 vector-set identity: `dff01501326305709dc1eda3241a92458ce17a3461b6dd254c7f8f841a6932b1`;
- `test-vectors/names_v1_wire.json` SHA-256:
  `0aeb8795386c47f235375a648b0a3c512e75c8f3d9a5b40ae8c224d0807ef40a`.

The identities were regenerated by the pinned
`orchard::circuit::state_note_binding::keygen()` path and checked by
`frozen_state_note_verifying_key_identities_are_stable`. The wire generator
was run with:

```text
cargo test --locked --offline -p coppice-names --test names_v1_wire_vectors \
  -- --ignored --nocapture generate_names_v1_wire_vectors
```

and its JSON output matched `names_v1_wire.json` byte-for-byte. The current
source pins `orchard-coppice-zakura` `zakura-port` at
`0e09398970130b9510ce5011129acafc5039e79f`.

The live run used
`zcash-devtool/scripts/live-qualification.sh --phase 2 --keep-state` from
the Zakura-backed stack:

- `coppice-names` source baseline: `389649c438c7b17cfd8f940719a705f2451d48cc`;
- `zcash-devtool`: `e50bff17a1f41aca9cc190e60142878dfd6020ca`;
- `orchard-coppice-zakura`: `0e09398970130b9510ce5011129acafc5039e79f`;
- Zakura: `f892b9074002a04a678ef2365ec7658795796572`;
- Zaino: `b819583a1a6663a01cb7681ac5b5fc2a174596a0`;
- preserved evidence: `/tmp/coppice-names-v1-live.WD5aGn`.

Canonical lifecycle evidence (all verification steps reported
`NAMES_FULL_FRESH_MATCH=yes`):

- COMMIT `7b67f452a1ca75affa1dcc05e6d1471781a23d41c6b03ab92f450a51dac4b262`
  was accepted at height 14, transaction index 1, operation index 0;
- REVEAL `a7e3f9f8d43586daa8379ee8014808072314bf6d226693dd4ffecdd14c2dd107`
  was accepted at scheduled height 19, with replay and FreshResolver both
  returning `Active`;
- UPDATE `d1b89e2559b7f76a32959e8789e5d15642040c62c41e69cfa9fdb7d60ade5d0f`
  was accepted at height 20, with both paths returning `Active`;
- RENEW `d8e1e7f32054434fa6f10bd03604aeed8f64f32e897d13b3b5a372477e21efa2`
  was accepted at scheduled height 30, with both paths returning `Active`;
- RELEASE `7a4286659bde66ec3f891f5f4f0dede996ed7443a5cde3433854e9482a2f0376`
  was accepted at height 31, with both paths returning `Released`.

The exact claimability edge was also checked: `Released` at height 34 and
`Expired` at height 35 (`terminal_height 31 + reuse_delay 4`), with replay and
FreshResolver agreeing at both heights. The run measured 4,704-byte Names
proofs, 5,118-byte CNV1 REVEAL envelopes, 5,011-byte CNV1 UPDATE/RENEW/RELEASE
envelopes, and 11 CPV1 frames for each state operation. These are recorded
qualification observations; proof-size and performance optimization remains
outside this release-finalization campaign.
