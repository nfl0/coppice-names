import Zcash.Coppice.Names

/-!
# Pinned Coppice Names formal entrypoint

The authoritative model and proofs live in the Ironwood fork pinned by
`lakefile.toml`. Importing them here makes that exact revision the complete
build surface of this small consumer project.
-/
